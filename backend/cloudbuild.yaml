steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-f',
        'backend/Dockerfile',
        '-t',
        'gcr.io/$PROJECT_ID/sb-ecom-backend:$COMMIT_SHA',
        'backend'
      ]

  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'push',
        'gcr.io/$PROJECT_ID/sb-ecom-backend:$COMMIT_SHA'
      ]

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run',
        'deploy',
        'sb-ecom-backend',
        '--image',
        'gcr.io/$PROJECT_ID/sb-ecom-backend:$COMMIT_SHA',
        '--region',
        'europe-west1',
        '--platform',
        'managed',
        '--quiet'
      ]

images:
  - 'gcr.io/$PROJECT_ID/sb-ecom-backend:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY